using UnityEngine;

namespace RTS
{
    public class Whells : MonoBehaviour // Rotate whells.
    {
        [SerializeField] private GameObject[] Whell;
        public bool hasRotate;
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }

}